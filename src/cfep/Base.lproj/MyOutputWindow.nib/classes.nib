{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {printDocument = id; }; 
            CLASS = MyOutputWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                inputCounterField = NSTextField; 
                textViewOut = NSTextView; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}