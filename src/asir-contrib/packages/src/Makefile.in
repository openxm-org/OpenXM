# $OpenXM: OpenXM/src/asir-contrib/packages/src/Makefile.in,v 1.5 2005/04/08 06:25:12 takayama Exp $
# targets
# all : 
#     do nothing
# install :
#     copy all files to $(OpenXM_lib/asir-contrib)
# clean :

OpenXM_ROOT=@prefix@
OpenXM_lib=$(OpenXM_ROOT)/lib
## If you add a new source to be installed, add in the list below.
SOURCE=*.rr *.h

all :

clean :
	-rm -rf config.log config.status autom4te.cache
install : clean-for-install
	cp $(SOURCE)  $(OpenXM_lib)/asir-contrib		
# Installation of libraries in directories. Todo: write a better method.
	mkdir -p $(OpenXM_lib)/asir-contrib/todo_parametrize		
	cp -r todo_parametrize/*.rr  $(OpenXM_lib)/asir-contrib/todo_parametrize		
	mkdir -p $(OpenXM_lib)/asir-contrib/y_prime
	cp -r y_prime/*.rr y_prime/Makefile  $(OpenXM_lib)/asir-contrib/y_prime
	mkdir -p $(OpenXM_lib)/asir-contrib/nk_mora
	cp -r nk_mora/*.rr $(OpenXM_lib)/asir-contrib/nk_mora
	mkdir -p $(OpenXM_lib)/asir-contrib/fj_curve
	cp -r fj_curve/*.rr $(OpenXM_lib)/asir-contrib/fj_curve

clean-for-install :
	/bin/rm -rf $(OpenXM_lib)/asir-contrib
	mkdir $(OpenXM_lib)/asir-contrib
	touch $(OpenXM_lib)/asir-contrib/DO_NOT_EDIT_THIS_DIRECTORY._ALL_FILES_ARE_AUTOMATICDALLY_GENERATED.
